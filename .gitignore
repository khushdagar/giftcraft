node_modules
.next
.turbo
dist
build
out
.env
.env.local
.env.*.local
# Backup/scratch copies of env files — these previously slipped past the rules
# above (e.g. .env.local.bak) and leaked live secrets into git history.
.env*
*.bak
.DS_Store
*.log
.vercel
coverage
.vscode

# TypeScript incremental build cache — generated, machine-specific,
# rewritten on every tsc run. Never commit (it cannot be merged).
*.tsbuildinfo
